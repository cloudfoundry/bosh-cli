#!/usr/bin/env ruby

GLOBAL_PROPERTY="<%= p('fake_cpi_default_property.second_level') %>"
JOB_PROPERTY="<%= p('fake_cpi_specified_property.second_level') %>"
IP="<%= spec.networks.default.ip %>"

input = ARGF.read

if input =~ /create_stemcell/
  puts '{"result":"fake-stemcell-cid","log":"fake-log"}'
else
  puts '{"result":"fake-vm-cid","log":"fake-log"}'
end
